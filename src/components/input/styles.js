import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    maxWidth: 110,
    fontSize: 25,
    borderColor: 'black',
    marginTop:20,
    borderBottomWidth:1,
    textAlign:'center',
  },
});
